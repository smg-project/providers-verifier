"""DeepSeek vendor profile: placeholder. To enable, mirror vendors/zai:
client factories (OpenAI-compatible base URL https://api.deepseek.com, env DEEPSEEK_API_KEY),
vendor_body for `thinking`/reasoning fields, and vendor cases (reasoning_content round-trip,
prefix completion, JSON mode, function calling). Then add `PROFILE = VendorProfile(...)`."""
