# Third-party test data

- `minimax/sample.jsonl` — MiniMax-Provider-Verifier tool-call battery (102 requests with `expected_tool_call` labels). MIT, see `minimax/LICENSE`.
- `walle/<suite>/valid.jsonl` — Moonshot walle validator JSON-Schema cases as snapshotted by Kimi-Vendor-Verifier (212 schemas). MIT, see `walle/LICENSE-Kimi-Vendor-Verifier` and `walle/SOURCE.md`.
- `media/` — small video/image fixtures from MiniMax-Provider-Verifier `m3_format_check` (MIT); images for the resolution/count/size tiers are generated at run time.
